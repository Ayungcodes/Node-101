import os from "os";

// userInfo()
console.log(os.userInfo());

// totalmem()
console.log(os.totalmem());

// freemem()
console.log(os.freemem());

// cpus() gives us information about the cpu
console.log(os.cpus());
